<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginActivity"></activity>
        <activity android:name=".markActivity" />
        <activity android:name=".mark" />
        <activity android:name=".subjectActivity" />
        <activity android:name=".presentActivity" />
        <activity android:name=".diviActivity" />
        <activity android:name=".yearActivity" />
        <activity android:name=".branchActivity" />
        <activity android:name=".clgActivity" />
        <activity android:name=".takeatActivity" />
        <activity android:name=".signupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>